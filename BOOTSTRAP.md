# BOOTSTRAP.md

This workspace is already initialized for OpenClaw-based Handshake automation.

Use:

1. `./scripts/openclaw_setup.sh`
2. Configure `config/application.yaml` and `config/qa_defaults.yaml`
3. `./scripts/openclaw_apply_handshake.sh 25 dry-run`

Delete this file once setup is complete on your machine.

